import styled from 'styled-components';
import MaskedInput from 'react-text-mask';

export const TheInput = styled(MaskedInput)`
  width: 70px;
  border: none;
  text-align: center;
  font-size: 14px;
  font-weight: bold;
  color: #555;
`;
